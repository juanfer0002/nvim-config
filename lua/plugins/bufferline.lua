return {
  "akinsho/bufferline.nvim",
  opts = {
    options = {
      -- This ensures the tab indicators always show on the right
      show_tab_indicators = true,
      -- This ensures the tab bar section itself doesn't hide
      always_show_bufferline = true,
    },
  },
}
